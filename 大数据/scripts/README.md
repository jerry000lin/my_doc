# 大数据脚本

## 定位

这个目录存放数据访问、元数据扫描、权限检查和结果查看相关脚本。脚本属于工具资产，需要能说明运行前提和输出含义。

## 当前脚本

| 脚本 | 用途 |
| --- | --- |
| `pyspark_扫库.py` | 使用 PySpark 执行库表扫描。 |
| `查看扫库结果.py` | 查看或分析扫库结果。 |
| `获取所有有权限的库表.py` | 获取当前账号可访问的库表清单。 |

## 维护规则

- 脚本名避免空格，便于命令行调用。
- 涉及账号、库名、路径等敏感配置时，不写死在脚本中。
- 新增脚本时同步补充用途、输入、输出和依赖说明。
